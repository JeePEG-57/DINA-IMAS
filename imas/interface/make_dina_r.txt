F90_gfortran=gfortran -w
COPTS_gfortran = -O0 -freal-4-real-8 -cpp -fPIC -fno-second-underscore -ffree-line-length-none -g

#IMAS
#COPTS_gfortran += `pkg-config imas-gfortran --cflags`
#LIBS_gfortran=`pkg-config imas-gfortran --libs`

f2c= gfortran -w  -ffixed-line-length-none -fd-lines-as-comments -fno-automatic -fPIC

DINATRANSP = 

FORTRANWF = test_dina2 
TARGETS += $(FORTRANWF) dina_imas_90.a  kmc.a

DINADIR := ../../src/scenario
CONTROLDIR := ../../src/controllers
ASTRADIR := ../astra_transp

LIB_SOURCEDINA := out42_for.o a_main2_dina.o

LIB_SOURCECTRL := n_kav.o out42_for.o main_1l.o



all: $(TARGETS)

#--------------------- gfortran ------------------------

test_dina2: a_main2_dina.o dina_imas_90.a kmc.a 
	@echo "   * $^ -> $@"
	$(F90_gfortran) -o $@ $^ $(LIBS_gfortran)


	@echo "   * $^ -> $@"
	$(F90_gfortran) -o $@ $^ $(LIBS_gfortran)

%.o: %.f90
	@echo "   * $^ -> $@"
	$(F90_gfortran) $(COPTS_gfortran) -c -o $@ $^

%.o: %.f
	@echo "   * $^ -> $@"
	$(f2c) -c $<

dina_imas_90.a: $(DINADIR)/dina_90.a $(LIB_SOURCEDINA)
	@echo "   * $^ -> $@"
	@cp $< $@
	ar -rcs $@ $(LIB_SOURCEDINA)

kmc.a: $(CONTROLDIR)/kmc/kmc.a $(LIB_SOURCECTRL)
	@echo "   * $^ -> $@"
	@cp $< $@
	ar -rcs $@ $(LIB_SOURCECTRL)

clean:
	rm -f *.o
	rm -f *.a
	rm -f $(TARGETS)
